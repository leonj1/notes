package sdk

// Version is the semantic version of this SDK release. It must be kept in
// sync with the Git tag used to publish the module (e.g. tag sdk/v0.1.0
// corresponds to Version = "v0.1.0"). The release workflow enforces this
// pairing on every tag push.
const Version = "v0.1.0"
